<h2>All Users</h2>

  <% @users.each do | user |%>
    <tr>
      <td><%= user.username %></td>
      <td><%= user.name %></td><br />
    <tr>
  <% end %>

<%= link_to 'New user', new_user_path(@user) %>
